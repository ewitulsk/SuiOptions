# Vendored from MystenLabs/deepbookv3 @ e276939f0613b98d8dcfa818ada3612dbbeb386b
# (source-identical to the v8.0.0 tag) — the exact rev our self-hosted testnet
# deepbook (0x725378…f42431) was published from. Vendored because the resolver
# takes a dependency's on-chain addresses from ITS OWN Published.toml; a git
# dep would silently link the official DeepBook instead of ours.
[package]
name = "deepbook"
edition = "2024.beta"
version = "0.0.1"

[dependencies]
token = { local = "../token" }

[addresses]
deepbook = "0x0"
