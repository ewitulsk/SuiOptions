[package]
name = "pricing"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
tracing = { workspace = true }
chrono = { workspace = true }

# Exact-to-~1ulp erfc for the standard normal CDF.
libm = "0.2"
