# staging-mm-bot — staging deployment. In-compose service URLs come from
# the environment (ORDERBOOK_URL / TOKEN_INFO_URL / ORACLE_URL); this file
# holds the trading knobs.

health_addr = "0.0.0.0:8085"

network = "testnet"

max_fee_bps = 50

[funding]
enabled = true
check_interval_secs = 60

# Vault-direct mode (below): these are VAULT free-balance floats, topped
# up by faucet mint + attested deposit (SO-375).
[funding.targets]
TUSDC = 1_000_000_000_000 # $1M at 6 decimals
TBTC = 1_000_000_000      # 10 TBTC at 8 decimals
TSUI = 100_000_000_000_000 # 100k TSUI at 9 decimals
TWAL = 100_000_000_000_000 # 100k TWAL at 9 decimals

[quoting]
ttl_secs = 90
refresh_secs = 5
requote_drift_bps = 5
escrow_utilization = 0.8
# Batched on-chain cancel_up_to sweep cadence. Replaced orders expire after
# ttl_secs anyway, so this is a slow belt, not the primary cancel.
watermark_interval_secs = 3_600

[[quoting.levels]]
bps = 10
lots = 1

[[quoting.levels]]
bps = 25
lots = 2

[[quoting.levels]]
bps = 50
lots = 4

[pyth]
max_price_age_ms = 5_000
max_publish_lag_ms = 10_000
max_conf_bps = 0

# ── Vault-direct escrow (SO-372) ────────────────────────────────────────
# Quote as the direct-escrow vault's delegated signer: orders name the
# identity BM below and settle from the vault's free balances per fill.
# Ceremony ran 2026-08-11 (trading-vault-smoke --direct-escrow): custody
# 0xff60855337b5cc45fe6eda2dde1decf94a3649cd6d9afd0495e2cba65271b4e8,
# quote adapter enabled, this bot's wallet delegated as signer. Remove
# this section to fall back to the funded-BM mode.
[vault_direct]
manager_id = "0xc719bb4cd8819ab03a0efe356cc2398af692425a6f9bd0d298f568b7bae60373"
vault_id = "0xf3f228542d94d8cb9ceaf70da892f156acfadeeacac0cdbf733059aec84bd9d3"
api_url = "http://api-service:9003"
buffer_bps = 1_000
