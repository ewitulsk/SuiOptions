# Quoting service config — staging env (devnet).

bind_addr            = "0.0.0.0:9002"
indexer_url          = "ws://indexer:9001/"
rfq_window_ms        = 2000
ping_interval_secs   = 15
protocol_id          = "sui-options-protocol-v0"
