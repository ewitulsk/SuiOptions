# airdrop-bot secrets. Copy to secrets.toml for local dev; deployed envs
# render this from AWS Secrets Manager (options/<env>/airdrop-bot) via
# render-secrets.sh.

# Discord application public key (hex) — General Information page of THIS
# bot's application in the developer portal (a separate application from
# social-bot's).
discord_public_key = "REPLACE_ME"
