# api-service config (local dev).
#
# Reads protocol state from the indexer's GraphQL API and serves HTTP to the frontend.

bind_addr        = "127.0.0.1:9003"
indexer_graphql_url = "http://127.0.0.1:9002/graphql"

# Frontend dev origins. Use ["*"] for permissive dev only.
allowed_origins  = ["http://localhost:5173", "http://127.0.0.1:5173"]

# Which Sui network slot in `deployments.json` to read from. Drives the
# coin-type → {symbol, decimals} lookup used to enrich bucket payloads.

# token-info public API. Catalog (coin-type -> {symbol, decimals}) is
# fetched from here at boot.
token_info_url   = "http://127.0.0.1:9005"
