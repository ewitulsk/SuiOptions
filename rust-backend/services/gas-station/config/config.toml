# gas-station config (local dev).
#
# Sponsors user transactions by paying their gas. The sponsor key is read from
# the secrets TOML (`--secrets`, default services/gas-station/config/secrets.toml)
# under the `[sui]` slot for `network` below; copy secrets.example.toml to
# secrets.toml and fill it in.

environment = "dev"

bind_addr   = "127.0.0.1:9009"
network     = "testnet"

allowed_origins = ["http://localhost:5173", "http://127.0.0.1:5173"]

# 1 SUI. Below this the toggle defaults off in the UI.
min_balance_threshold_mist = 1000000000

# 0.5 SUI cap per sponsored tx (also the dry-run budget).
max_gas_budget_mist = 500000000
min_gas_budget_mist = 1000000
gas_budget_buffer_bps = 2500

# token-info supplies the sponsorable packages: the protocol package plus every
# supported token's coin-type package.
token_info_url = "http://127.0.0.1:9005"

# Extra packages to sponsor beyond what token-info reports. Off-catalog only.
additional_allowed_packages = []

# CCTP bridge burn PTB sponsorship (frontend tx/bridge.ts). Set to Circle's
# TokenMessengerMinter package for this network to sponsor the burn.
# cctp_token_messenger_package = "0x31cc14d80c175ae39777c0238f20594c6d4869cfab199f40b69f3319956b8beb"  # testnet (mainnet: 0x2aa6c5d56376c371f88a6cc42e852824994993cb9bab8d3e6450cbe3cb32b94e)
